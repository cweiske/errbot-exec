**********************
Exec plugin for errbot
**********************

Execute an external command when the bot is talked to.

=============
Configuration
=============
In ``config.py``::

    EXEC = {
        'command': u'echo'
    }
